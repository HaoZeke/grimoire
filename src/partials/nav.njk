<div class="gridNav">
<div id="toggle">
<div class="burger">
+
</div>
</div>
<!-- gridNav -->
</div>

<div class="gridMenu">
<div class="menu closed">
<div class="navList">
{% for item in collections %}
				<div class="navItem">
					<a href="{{rootPath}}/{{item.path}}">{{item.title}}</a>
				</div>
{% endfor %}
    <ul class="recent">
    {% for article in articles %}
      <li>
        <div class="title"><a href="/{{rootPath}}{{ article.path }}">{{ article.title }}</a></div>
        <div class="date">{{ article.date }}</div>
      </li>
    {% endfor %}
    </ul>
<!-- navList -->
<!-- menu closed -->
</div>
<!-- gridMenu -->
</div>
</div>

{%if pdf %}
<div class="gridBadges sticky-top">
	<div class="badgeList">
		<a class="pdf">
			<img src="https://img.shields.io/badge/Get-PDF-2E3440.svg?style=for-the-badge" alt="Get this as a PDF" />
		</a>			
	</div>
</div>
{% endif %}